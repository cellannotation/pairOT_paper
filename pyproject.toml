[build-system]
requires = ["setuptools>=62.1", "cython>=3.0.11"]
build-backend = "setuptools.build_meta"

[project]
name = "pairot"
version = "0.1.0"
description = "Connecting cell types accross datasets."
authors = [{name = "Felix Fischer", email = "fe.fischer@icloud.com"}]
readme = "README.md"
requires-python = ">=3.10"
license = {file = "LICENSE"}
dependencies = [
    "anndata>=0.10",
    "jax>=0.4.25",
    "numpy>=1.26.4",
    "pandas>=2.2.1",
    "scanpy>=1.9.8",
    "tqdm>=4.66.2",
    "scipy>=1.12",
    "ott-jax>=0.4.9",
    "plotly>=5.21.0",
    "matplotlib>=3.8.4",
    "scikit-learn>=1.0",
]

[[tool.setuptools.ext-modules]]
name = "pairot.de_testing.auroc"
sources = ["pairot/de_testing/auroc.pyx"]

[tool.setuptools.package-data]
"pairot.de_testing" = ["resources/*"]

[project.optional-dependencies]
de_testing = [
    "cython>=3.0.11",
    "joblib>=1.4.2",
    "rpy2==3.5.11",
]

[tool.setuptools.packages.find]
exclude = ["notebooks", "scripts"]

[project.urls]
Repository = "https://github.com/cellannotation/pairOT/tree/devel"
